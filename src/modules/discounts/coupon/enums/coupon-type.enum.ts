export enum CouponType {
    PUBLIC = 'public',        // cualquiera puede usarlo
    PRIVATE = 'private',      // asignado a usuario específico
    UNIQUE = 'unique',        // código único por usuario
  }